export type Story = {
  slug: string;
  title: string;
  excerpt: string;
  content: string;
  date: string;
};

// No verified success stories are published yet — replace with real
// stories from the field as they're written.
export const stories: Story[] = [];
