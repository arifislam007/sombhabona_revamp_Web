# env files (never commit real secrets)
.env
.env.local
.env.*.local
!.env.example

# editor/tooling local state
.claude/

# dependency / build artifacts (also ignored per-package, kept here as a safety net)
node_modules/
.next/
/lib/generated/prisma

# OS
.DS_Store

# raw photo archive (source material — selected/optimized copies live in apps/web/public/gallery/)
/Audio Visual/
